---
description: Contexto del proyecto infinity-landing (JACKO landing, scroll-scrub)
alwaysApply: true
---

# infinity-landing

- Vite + React 19 + TypeScript; animaciones con `motion/react` y canvas scroll-scrub.
- Hero: `ScrollEscudoFase2Bridge` + frames en `src/generated/miPersonajeFrameUrls.ts`.
- Secciones: `#inicio`, `#pricing`, `#register`; dock tras completar intro.
- Agentes: ver `AGENTS.md`; skills infinity + UI/UX Pro Max (`@ui-ux-pro-max`, `@design-system`, `@brand`, etc.) en `.cursor/skills/`.
- Cambios mínimos; copiar estilo existente (2 espacios, imports desde rutas del proyecto).
- Tras cambios relevantes: `npm run verify`.
